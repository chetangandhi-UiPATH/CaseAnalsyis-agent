[project]
name = "case-analysis"
version = "0.0.37"
description = "CIP Case Analysis — Salesforce support case analyser deployed as a UiPath Coded Agent"
authors = [{ name = "Chetan Gandhi", email = "chetan.gandhi@uipath.com" }]
dependencies = [
    "uipath-langchain>=0.10.0",
    "anthropic>=0.40.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]
requires-python = ">=3.11"
